import React from "react";

const useSetDrop = (player, setPlayer) => {
  let refresh;
  //   if (!refresh) {
  //     refresh = setInterval(() => {
  //       console.log("test");
  //       setPlayer((prevState) => {
  //         let temp = { ...prevState };
  //         temp.position.row += 1;
  //         return temp;
  //       });
  //     }, 10000);
  //   }
};

export default useSetDrop;
